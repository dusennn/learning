import java.util.*;

/**
 * 堆：大顶堆、小顶堆
 */
public class A1 {


	/**
	 * 把一个无序数组整理成小顶堆
	 * 1. 堆可以使用数组表示，类似于完全二叉树
	 * 假如父节点位于数组的第 i 位，左孩子 2i+1, 右孩子 2i+2
	 * 2. 最小的值永远在树顶，树顶的左孩子一定小于右孩子
	 * 3. 
	 */
	public int[] minHeapSort(int[] nums) {
		int[] heap = new int[nums.length];


	}
}

